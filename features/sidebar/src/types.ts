import { type MdiReactIconComponentType } from 'mdi-react';

export type IconType = MdiReactIconComponentType;
export type ButtonType = 'open' | 'close' | 'toggle';
export type SidebarPosition = 'left' | 'right';
export type OpenType = {
  open: boolean;
};
