import DocCardList from '@theme/DocCardList';

# Features Included in the Application

The application was split into "features" (self-contained functional parts) to be integrated into the application.

<DocCardList />