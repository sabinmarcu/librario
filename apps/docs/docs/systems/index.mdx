import DocCardList from '@theme/DocCardList';

# Systems built alongside the application

We'll be talking here about systems built to support the app, and pieces of code that could be redistributed and reused across projects.

I will be skipping `libs/utils` and `libs/hooks`, since they're pretty self-explanatory.

<DocCardList />